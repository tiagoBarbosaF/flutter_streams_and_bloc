import 'package:flutter/material.dart';

final mybankTheme = ThemeData(
  colorScheme: ColorScheme.fromSwatch(
    primarySwatch: Colors.teal,
  ).copyWith(
    secondary: Colors.tealAccent[900],
  ),
);
